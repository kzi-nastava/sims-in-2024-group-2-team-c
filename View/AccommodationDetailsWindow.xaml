<Window x:Class="BookingApp.View.AccommodationDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BookingApp.View"
        mc:Ignorable="d"
        Title="AccommodationDetailsWindow" Height="450" Width="800"
        ResizeMode="NoResize" WindowStyle="SingleBorderWindow">
    <Grid>
        <StackPanel Margin="10">
            <TextBlock Text="Enter Trip Details" FontWeight="Bold" Margin="0 0 0 10"/>

            <TextBlock Text="Start Date:" Margin="0 0 0 5"/>
            <DatePicker x:Name="StartDatePicker" Margin="0 0 0 10"/>

            <TextBlock Text="End Date:" Margin="0 0 0 5"/>
            <DatePicker x:Name="EndDatePicker" Margin="0 0 0 10"/>

            <TextBlock Text="Stay Duration (Days):" Margin="0 0 0 5"/>
            <TextBox x:Name="StayDurationTextBox" Margin="0 0 0 10"/>

            <!-- Dodajemo Horizontalni StackPanel za dugmad -->
            <StackPanel Orientation="Horizontal">
                <Button Content="Check" Click="CheckButton_Click" Width="100"/>
                <Button Content="Go back" Click="GoBackButton_Click" Width="100" Margin="10 0 0 0"/>
            </StackPanel>
        </StackPanel>

    </Grid>
</Window>
