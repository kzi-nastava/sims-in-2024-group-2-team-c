<Page x:Class="BookingApp.WPF.View.TouristView.TouristUserView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BookingApp.WPF.View.TouristView"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="430"
      Title="TouristUserView" Background="White">
    <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden">
    <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height="20"></RowDefinition>
                <RowDefinition Height="40"></RowDefinition>
                <RowDefinition Height="70"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="1"  FontSize="15"   Text="{Binding Username}"></TextBlock>

            <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="10"  Background="#65b565">
                <TextBlock Text="Vouchers" FontSize="15" FontWeight="Medium" VerticalAlignment="Center" Width="322" Margin="10"/>
                <Button Content="View" Click="View_Click" Height="30" Width="50"></Button>
            </StackPanel>

        </Grid>
    </ScrollViewer>
</Page>
